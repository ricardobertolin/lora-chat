{
  "name": "lora-chat-webapp",
  "private": true,
  "type": "module",
  "description": "Browser client for the lora_chat firmware. Only used so node treats the .js files as ES modules when running tests; the app itself needs no build step and no dependencies.",
  "scripts": {
    "test": "node --test test/"
  }
}
